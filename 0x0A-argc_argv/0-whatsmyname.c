#include "main.h"
#include <stdio.h>

/**
 * main - Entry point name
 * @argc: count
 * @argv: vector
 *
 * Retrun: Always 0.
 */

int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);

	return (0);
}
